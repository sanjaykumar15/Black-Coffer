<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BlackCoffer" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_bg</item>
        <item name="colorPrimaryVariant">@color/primary_bg</item>
        <item name="colorOnPrimary">@color/grey</item>
        <item name="android:windowFullscreen">false</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/main_bg</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="switchTheme" parent="Theme.AppCompat.Light">
        <item name="colorSwitchThumbNormal">@color/l_grey</item>
        <item name="colorControlActivated">@color/main_bg</item>
    </style>
    <style name="sliderTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/main_bg</item>
    </style>
</resources>